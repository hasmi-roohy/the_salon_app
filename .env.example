# Development database behavior. Never enable synchronization in production.
DB_SYNCHRONIZE=true
DB_LOGGING=false

# MongoDB is currently optional and is not required for virtual try-on.
MONGO_ENABLED=false
MONGO_URI=mongodb://localhost:27017/salon_ai_logs
